<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.alignsolution.customermobileapplication">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".RegisterOTPActivity" />
        <activity android:name=".RegisterOTPPopUpActivity" />
        <activity android:name=".RegisterTeamCondition" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".ForgetPasswordActivity" />
        <activity android:name=".UserProfileActivity" />
        <activity android:name=".UserProfileEditActivity" />
        <activity android:name=".PolicyListActivity" />
        <activity android:name=".PolicyDetailActivity" />
        <activity android:name=".ProductPromotionListActivity" />
        <activity android:name=".ProductPromotionDetailActivity" />
        <activity android:name=".ClaimPolicyListActivity" />
        <activity android:name=".ClaimPolicyActivity" />
        <activity android:name=".ClaimStep1Activity" />
        <activity android:name=".ClaimStep2Activity" />
        <activity android:name=".ClaimStep3Activity" />
        <activity
            android:name=".ClaimStep3ActivityPopUp"
            android:label="@string/app_name"
            android:screenOrientation="landscape"></activity>
        <activity android:name=".PayCustomerAcitivity" />
        <activity android:name=".PayDetailActivity" />
        <activity android:name=".OtherMenuActivity" />
        <activity android:name=".OtherCallCenterActivity" />
        <activity android:name=".ComplainAcitivity" />

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".activity_other_map"
            android:label="@string/title_activity_other_map"></activity>
    </application>

</manifest>